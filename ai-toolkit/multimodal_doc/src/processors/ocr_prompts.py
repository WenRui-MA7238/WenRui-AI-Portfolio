"""
Task-specific prompt templates for multimodal document understanding.
"""


OCR_PROMPT = """请提取图片中的所有文字内容，保留原始段落和排版。
如果是表格，请输出 Markdown 表格。
如果是表单，请输出字段名和值的对照。"""


TABLE_PROMPT = """请提取图片中的表格内容，并以 JSON 格式返回。
JSON 格式为：{"headers": [...], "rows": [[...], ...]}。
不要包含任何额外说明。"""


INVOICE_PROMPT = """请提取图片中发票的关键字段，以 JSON 格式返回：
{
  "发票号码": ..., "开票日期": ..., "购买方": ..., "销售方": ...,
  "金额": ..., "税额": ..., "价税合计": ..., "备注": ...
}
如果某字段未找到，值设为 null。不要包含额外说明。"""


DESCRIBE_PROMPT = """请用一段简洁的话描述这张图片的内容，包括主要物体、文字、布局和潜在含义。"""


RECEIPT_PROMPT = """请提取图片中收据/小票的关键字段，以 JSON 格式返回：
{
  "商家": ..., "日期": ..., "商品": [...], "总金额": ..., "币种": ...
}
如果某字段未找到，值设为 null。不要包含额外说明。"""


ID_CARD_PROMPT = """请提取图片中身份证的关键字段，以 JSON 格式返回：
{
  "姓名": ..., "性别": ..., "民族": ..., "出生": ..., "住址": ...,
  "公民身份号码": ..., "签发机关": ..., "有效期限": ...
}
如果某字段未找到，值设为 null。不要包含额外说明。"""
